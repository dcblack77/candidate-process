{
  "name": "candidate-process",
  "version": "0.1.0",
  "private": true,
  "description": "Sistema local de evaluación de candidatos (ver BLUEPRINT.md)",
  "engines": {
    "node": ">=22.0.0",
    "pnpm": ">=11.0.0"
  },
  "scripts": {
    "dev": "pnpm --filter api dev",
    "build": "pnpm -r build",
    "test": "pnpm -r test"
  }
}
